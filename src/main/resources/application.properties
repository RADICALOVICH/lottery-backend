# Application
app.isProd=false
app.port=8080

# Database
db.url=jdbc:postgresql://localhost:5433/lottery
db.user=lottery
db.password=lottery_dev_password
db.poolSize=10

# BCrypt
bcrypt.cost=12

# Scheduler
draw.scheduler.intervalSeconds=60

